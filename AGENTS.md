# AGENTS.md

## 基本方針

このリポジトリでは、PC用Web版 `study-pm` を開発する。

開発では、要件定義、基本設計、実装、レビューを段階的に進める仕様駆動開発の考え方を取り入れる。

仕様駆動開発は、アプリのMVP機能ではなく、開発者の学習目的・開発方針として扱う。

## 成果物とAI指示ファイル

- `docs/requirements/` は要件定義の成果物を管理する。
- `docs/basic-design/` は基本設計の成果物を管理する。
- `docs/detailed-design/` は詳細設計の成果物を管理する。
- `docs/ui-mock/` はUIモックの設計・確認資料を管理する。
- `docs/development/` は開発方針を管理する。
- `.agents/skills/` はCodexに作業させるためのAgent Skillを管理する。
- レビュー結果は、対象成果物の `review/` 配下に置く。
- レビュー方法は、`.agents/skills/` に置く。

## AI作業ルール

- AI出力は正式成果物ではなく、人間が確認するたたき台とする。
- 要件定義Skillと基本設計Skillは、原則として先に提案を出し、ユーザーが明示的に依頼した場合のみファイルを変更する。
- 詳細設計Skillも、原則として先に提案を出し、ユーザーが明示的に依頼した場合のみファイルを変更する。
- 要件、設計、実装を混同しない。
- MVP範囲を不用意に広げない。
- 詳細設計とテスト仕様書は、必要になった時点で作成する。
- 学習支援Skillとテスト設計Skillは現時点では作成しない。
