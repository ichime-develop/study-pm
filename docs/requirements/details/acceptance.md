<!--
doc-type: 要件定義
id-prefix: DSG-TBD
related: docs/requirements/summary/README.md, docs/requirements/details/functional.md, docs/requirements/details/glossary.md
-->

# 受入条件・制約・次工程

## 18. 受入条件

### 18.1 MVP 1

MVP 1は、個人用の学習計画・実績管理表として、プロジェクト、WBS、学習記録を登録し、予定工数、実績工数、進捗率を確認できる状態を完成条件とする。

1. ユーザーがアカウント登録、ログイン、ログアウトを行い、自分のデータだけを閲覧・更新できる。
2. ユーザーが学習プロジェクトを作成、編集、一覧表示、詳細表示、削除できる。
3. ユーザーがプロジェクト内に親タスクとタスクを作成し、タスクを親タスク配下または親なしで管理できる。
4. ユーザーがタスクに予定日、予定工数、進捗率を設定できる。
5. ユーザーがタスクに対して学習日、学習時間、任意メモを持つ学習記録を登録できる。
6. ユーザーが登録済み学習記録を確認し、編集・削除できる。
7. 未来日の学習記録を登録できない。
8. 親タスクを除くタスクから、プロジェクトの予定工数、実績工数、進捗率が正しく集計される。
9. 親タスクは予定日、予定工数、実績工数、進捗率、学習記録を持たず、計算対象外として扱われる。
10. 学習記録が存在するタスクは削除できないが、同一プロジェクト内での編集はできる。
11. プロジェクト一覧上部でユーザー単位の連続学習日数と総学習時間を確認できる。
12. MVP 2以降の進捗分析に利用するため、進捗履歴、計画履歴、プロジェクト期間履歴が保存される。進捗履歴は保存前後で進捗率が異なる場合のみ追加され、同一値を保存した場合は追加されない。
13. 文字数、工数、進捗率、日付、所有者の制約を満たさない操作は保存されず、理由が表示される。
14. WBSで選択中のタスクから学習記録登録を開始でき、保存後に再集計された実績工数がWBSへ反映される。
15. 学習記録画面では通常時に一覧を確認でき、必要な場合だけ登録・編集・削除の入力領域を表示できる。

### 18.1.1 プロジェクト管理

1. ユーザーが必須項目を入力してプロジェクトを作成すると、初期状態が未着手で保存される。
2. ユーザーがプロジェクト名、概要、開始日、目標終了日、状態を編集できる。
3. プロジェクト開始日が目標終了日より後の場合、保存できない。
4. タスクが存在しない、または未完了タスクが存在するプロジェクトは完了に変更できない。
5. ユーザーがキーワード、状態でプロジェクト一覧を絞り込める。
6. ユーザーが更新日時、開始日、目標終了日、進捗率でプロジェクト一覧を並び替えできる。
7. プロジェクト削除前に、削除対象と復元できないことを確認できる。
8. プロジェクトを削除すると、プロジェクト一覧とプロジェクト内機能の表示対象から除外される。
9. 削除したプロジェクトは復元できない。
10. プロジェクト概要では主要指標を確認でき、WBS、学習記録および後続MVPで追加する進捗分析をプロジェクト内機能として配置できる方針を確認できる。
11. プロジェクト一覧の「新規作成」から手動で作成またはAIと作成を選択でき、選択後はそれぞれ独立したフローへ進む。
12. 手動作成画面にAI作成への切り替えやAI機能の説明が表示されない。
13. 手動でプロジェクトを作成すると、プロジェクト概要画面へ遷移し、WBSタスクが0件の状態で表示される。
14. WBSタスクが未登録のプロジェクト概要では、基本情報、タスク0件、予定工数・進捗率が未算出であることが表示され、WBS作成へ進む導線が表示される。予定工数や進捗率は通常の数値として誤表示されない。
15. WBSタスクが0件の場合でも、WBS画面を表示し親タスク・親なしタスクの追加を開始できる。親タスクを作成した後、その親タスクを選択して配下タスクを追加できる。
16. WBSタスクの追加フォームは、WBSが空の場合もタスクがある場合も同じ右サイドパネルとして表示される。
17. WBSタスクが0件のプロジェクトでは、学習記録タブが無効になっており遷移できない。

### 18.2 MVP 2

1. 進捗分析画面で、JST当日を固定の基準日としてBAC、PV、EV、AC、SV、CV、SPI、CPIを確認できる。
2. 基準日はJST当日の日付として静的に表示される。
3. EVM指標をカード形式で確認できる。
4. MVP 1から保存された進捗履歴と現在有効なタスク予定工数を用いて、JSTの各日終了時点の進捗率に基づく日別EVが算出される。
5. 進捗分析画面で理想線と実績線を含むバーンダウンチャートを確認でき、実績線が `BAC - 日別EV` の残予定工数として表示される。
6. 予定工数、予定日、プロジェクト期間を変更した場合、日別EVとバーンダウンチャートが現在の計画値に基づいて再計算され、過去時点の計画値を用いた日別EVの再現は提供されない。
7. SPIは1.0未満の場合のみ「遅れ」、CPIは1.0未満の場合のみ「超過」と表示され、1.0以上の場合は状態ラベルが表示されない。
8. SVとCVは符号付き時間数として表示され、状態ラベルが表示されない。
9. 計算対象LEAFが0件、予定日未設定LEAFが存在する、またはBACが0の場合、EVM指標はすべて「-」として表示され、算出不可の理由を確認できる。それ以外では、PVが0の場合はSPI、ACが0の場合はCPIが「-」として表示され、BAC、PV、EV、AC、SV、CVは値が0の場合も0として表示される。
10. バーンダウンチャートでは、前倒し、予定どおり、遅れの閾値判定ではなく、理想残と実績残の差分日数が直接表示される。
11. BACが0、親タスクを除く計算対象タスクが存在しない、または予定日未設定LEAFが存在する場合、バーンダウンチャートが「-」として表示される。
12. 進捗履歴、計画履歴、プロジェクト期間履歴は、一覧表示ではなく進捗分析の計算または将来拡張用の内部データとして扱われる。
13. 遅延タスクと計画上の不整合を確認できる。

### 18.3 MVP 3

1. AI作成方法の選択画面で、Google Cloud VisionとOpenAIの利用、各サービスへ送信する情報・送信しない情報、データ利用方針への導線を確認できる。
2. AI利用同意を管理する専用画面、撤回API、同意状態の新規DB項目を追加せず、ユーザーが条件入力へ進む操作を表示内容の確認として扱う。
3. ユーザーが概要だけを入力する方法、目次テキストを直接入力する方法、教材目次画像を利用する方法のいずれからも学習計画作成を開始できる。
4. 教材目次画像はクライアントで最大10枚・合計50MB、サーバーで1画像10MB・生成依頼内のOCR入力元最大10件として検証される。最大10枚まで並び替えてOCRでき、画像単位の失敗を他の画像と区別して再試行できる。結合したOCR結果を学習項目候補の抽出前に確認・修正できる。
5. 教材目次画像はGoogle Cloud Visionへだけ送信され、OpenAIへ送信されず、処理後に永続保存されない。
6. OCR結果と貼り付け目次テキストの合計20000文字、概要5000文字、こだわり条件5000文字、AI送信テキスト合計30000文字の暫定上限を超えた場合、入力を切り捨てずに超過項目と修正方法が表示される。
7. 概要、目次テキスト、修正済みOCR結果のいずれからも、入力由来またはAI補足の由来を持つ学習項目候補を生成できる。
8. ユーザーが学習項目候補を確認・編集・追加・削除し、重点・通常・軽め・除外の優先度を変更できる。
9. ユーザーが候補一覧を一括確認でき、確認後に候補を変更すると確認済み状態が解除される。現在の候補一覧が確認済みで、除外されていない候補が1件以上ある場合だけWBS生成へ進める。
10. WBS生成時にOpenAIへ送信する学習内容は確認済み学習項目候補と確認済み数量条件に限定され、OCR結果と目次テキストの全文が再送されない。自然文の日程補足と確認済み数量条件が競合する場合、確認済み数量条件が使用される。
11. 学習項目候補の抽出とWBS下書きの生成が独立した非同期処理として管理され、処理状態、停止操作、失敗理由、再試行方法を確認できる。
12. 同一ユーザーがAI処理を実行中に新たなAI処理を開始した場合は拒否され、二重実行されない。
13. 停止要求後に外部サービスから結果が返った場合、その結果が学習項目候補またはWBS下書きとして保存されない。
14. 通信失敗、レート制限、一時的な外部サービス障害だけが自動再試行され、構造化出力の検証失敗は各処理1回だけ自動再生成される。
15. ユーザー別の日次生成上限に達した場合、生成が開始されず、再び利用可能になる時期を確認できる。
16. 開始日が終了日より後、学習可能時間が0、除外されていない候補が0件などの入力矛盾はAI処理前に検出され、理由と修正方法を確認できる。
17. 生成後の予定工数と学習可能時間に不整合がある場合、警告と、解除すべき条件の単一変更案を最大3件確認できる。
18. 通常生成と期限優先のどちらでも1日24時間を超える計画は生成されない。期限優先を選択した場合、確認済み候補と予定工数を維持し、24時間以内であれば希望する1日あたり学習時間を超える下書きを警告付きで生成できる。
19. AI出力はサーバーで必須項目、最大2階層、0.25時間単位、予定日、候補との対応を検証した後にWBS下書きとして表示され、直接プロジェクトやWBSへ保存されない。
20. ユーザーがWBS下書きのタスク名、タスク種別、親タスク、予定日、予定工数を直接編集し、タスクを削除できる。親タスク削除時は確認後に配下タスクも削除される。
21. ユーザーがWBS下書きから入力条件または学習項目候補へ戻り、変更後に再生成できる。
22. ユーザーがWBS下書きを確認して変換した場合だけ、サーバーで再検証したプロジェクトとWBSが1トランザクションで保存される。同じ下書きを2回変換できない。
23. AI処理に失敗した場合、OCR結果、修正後テキスト、入力条件、確認済み学習項目候補を失わず、対象の処理だけを再試行できる。
24. OCR・AIサービスの認証情報が未設定または外部サービスが利用できない場合、AI作成機能は利用不可として理由を表示し、手動作成とMVP 1・MVP 2の機能は利用できる。
25. 通常のCIでは有料のOCR・AIサービスを呼び出さず、スタブ、固定レスポンス、契約テストで主要な正常系・異常系を検証できる。
26. 「全300ページを1日10ページ」のような自然文の数量条件は単位・総量・1日量として表示され、ユーザーが確認・修正できる。確認済みの値からサーバーが必要日数を計算し、期限内に収まらない場合はWBS生成前に理由と解除候補が表示される。解釈不能な条件が残る場合はWBS生成へ進めない。
27. WBS下書きの各LEAFタスクについて、対応する学習項目候補を確認できる。この対応はプロジェクト変換後の通常WBSへ引き継がれない。

## 19. 制約・前提事項

- 初期リリースは個人開発として、段階リリースにより完成を優先する。
- 日本語UIを主対象とする。
- Flutterスマホアプリは別リポジトリで作成し、将来的にPC Web版と同じ主要機能を提供する。
- タイムゾーンはユーザー設定を導入するまで日本標準時（JST）を基準とする。
- 日付は日単位で管理し、最終MVPではタスクの開始時刻・終了時刻を管理しない。
- EVMは学習時間をコストとして扱うため、一般的な金額ベースのEVMとは表示上の意味が異なる。
- PVは休日を除外せず、全暦日へ均等配分する。

## 20. 基本設計で決定する事項

以下は要件ではなく、基本設計で比較・決定する。

| ID | 論点 |
| --- | --- |
| DSG-TBD-01 | 解決済み。PC Webフロントエンドの構成と採用技術は `docs/basic-design/tech-stack.md` に従う |
| DSG-TBD-02 | 解決済み。バックエンドの構成と採用技術は `docs/basic-design/tech-stack.md` に従う |
| DSG-TBD-03 | 解決済み。データベース製品は `docs/basic-design/tech-stack.md` に従う |
| DSG-TBD-04 | 一部解決済み。OCRはGoogle Cloud Vision、学習項目候補抽出とWBS生成はOpenAIを利用する。OpenAIの具体的なモデルは生成品質評価により選定し、運用設定で変更可能にする |
| DSG-TBD-05 | 方針解決済み。AI処理は受付時からの総期限と外部サービス呼び出し単位のタイムアウトを分離し、具体値は実測後に運用設定として決定する |
| DSG-TBD-06 | 未保存変更がある画面から離脱する場合の確認方法 |
| DSG-TBD-07 | バックアップの実行方式と復旧手順 |

## 21. 次工程

1. 最終MVP全体を見据えた画面構成とプロジェクト概要画面の情報設計を作成する。
2. 画面モックを確認し、MVP 1で実装する範囲と後続MVPへ送る範囲を切り分ける。
3. 基本設計書の構成を決定する。
4. 画面一覧、画面遷移、主要画面のワイヤーフレームを作成する。
5. エンティティ関連図とテーブル定義を作成する。
6. REST API一覧と主要なAPI仕様を作成する。
7. EVM、タスク集計、日別EV、バーンダウンなどの業務ロジックを設計する。
8. 技術スタック、開発環境、テスト方針、実装順序を決定する。
